{
  "name": "ricks-picks-docker",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build:server && npm run build:client",
    "build:server": "tsc -p tsconfig.json",
    "build:client": "cd client && npm run build",
    "start": "node dist/index.js",
    "db:push": "drizzle-kit push"
  }
}